% 原稿紙
\documentclass{article}
\usepackage{geometry}
\geometry{layout=letterpaper,landscape,hmargin=7.94mm,vmargin=4.7mm}

\pagestyle{empty}
\usepackage{tikz,ifthen}
\usetikzlibrary{calc}

\begin{document}
\begin{tikzpicture}[x=10mm,y=10mm,remember picture, overlay]
\tikzset{normal line/.style={gray, ultra thin}} 
\tikzset{border line/.style={gray, thick}} 

\node at (current page.south west){
  \begin{tikzpicture}[remember picture, overlay]
    \fill[fill=gray] (14.47,17.295)--(13.47,17.295)--(13.47,16.962)--(13.97,17.129)--(14.47,16.962)--cycle; % mid column top bracket
    \fill[fill=gray] (14.47,4.295)--(13.47,4.295)--(13.47,4.628)--(13.97,4.461)--(14.47,4.628)--cycle; % mid column bottom bracket

    \foreach[evaluate={\x=\c*1.3+0.77}] \c in {0, 1, ..., 9} { % each column of left half pane, from left to right
      \foreach \y in {0.795, 1.795, ..., 19.795} { % each column, from left to right
        \draw[style=normal line] (\x,\y) rectangle ({\x+1}, {\y+1});
      };
    };
    \foreach[evaluate={\x=\c*1.3+14.47}] \c in {0, 1, ..., 9} { % each column of right half pane, from left to right
      \foreach \y in {0.795, 1.795, ..., 19.795} { % each column, from left to right
        \draw[style=normal line] (\x,\y) rectangle ({\x+1}, {\y+1});
      };
    };
    \draw[style=normal line] (0.47,0.795) rectangle (27.47,20.795); % inner border
    \draw[style=border line] (0.37,0.695) rectangle (27.57,20.895); % outer border
  \end{tikzpicture}
};
\end{tikzpicture}
\newpage
\begin{tikzpicture}[x=10mm,y=10mm,remember picture, overlay]
\tikzset{normal line/.style={gray, ultra thin}} 
\tikzset{border line/.style={gray, thick}} 

\node at (current page.south west){
  \begin{tikzpicture}[remember picture, overlay]
    \fill[fill=gray] (14.47,17.295)--(13.47,17.295)--(13.47,16.962)--(13.97,17.129)--(14.47,16.962)--cycle; % mid column top bracket
    \fill[fill=gray] (14.47,4.295)--(13.47,4.295)--(13.47,4.628)--(13.97,4.461)--(14.47,4.628)--cycle; % mid column bottom bracket

    \foreach[evaluate={\x=\c*1.3+0.77}] \c in {0, 1, ..., 9} { % each column of left half pane, from left to right
      \foreach \y in {0.795, 1.795, ..., 19.795} { % each column, from left to right
        \draw[style=normal line] (\x,\y) rectangle ({\x+1}, {\y+1});
      };
    };
    \foreach[evaluate={\x=\c*1.3+14.47}] \c in {0, 1, ..., 9} { % each column of right half pane, from left to right
      \foreach \y in {0.795, 1.795, ..., 19.795} { % each column, from left to right
        \draw[style=normal line] (\x,\y) rectangle ({\x+1}, {\y+1});
      };
    };
    \draw[style=normal line] (0.47,0.795) rectangle (27.47,20.795); % inner border
    \draw[style=border line] (0.37,0.695) rectangle (27.57,20.895); % outer border
  \end{tikzpicture}
};
\end{tikzpicture}
\end{document}
